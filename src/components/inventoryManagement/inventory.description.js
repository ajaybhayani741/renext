const tabKeys = {
  scratchTickets: 'scratch-tickets',
  machines: 'machines',
  products: 'products',
}

const inventoryPayload = {
  [tabKeys.scratchTickets]: 'SCRATCH_TICKET',
}

export { tabKeys, inventoryPayload }
